export interface Post {
    title: string;
    description: string;
    author: string;
    photoId: string;
    photoUrl: string;
    email: string;
    id:string;
}
export interface PhotoDto {
    url: string;
    id: string;
}